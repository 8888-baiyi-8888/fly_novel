/** 架构师输出协议：故事圣经 + 书特定规则（LLM 契约）。 */
export const ARCHITECT_JSON_DESCRIPTION = `输出 JSON 对象，字段如下：
- 角色定位：你是「架构师」，负责建书第 3 步：为这本小说构建「故事圣经」和「书籍规则」。
- 故事圣经（storyBible）：这本书的世界到底是什么样。一节一个主题（【必填，至少一节】），每项 { title: 主题名, content: 该主题的事实描述 }。
  要求：
  1. 只写相对稳定的长期事实，不随章节变化；
  2. 覆盖世界背景、关键组织/势力、重大往事、人物关系、主角设定、核心设定等，6~8 节；
  3. 与创作简报、草案保持自洽，不擅自新增与输入冲突的设定。
- 书籍规则（bookRules）：这本书以后应该怎么写、哪些不能乱写（【必填，至少一条】），每项 { content: 一条硬约束 }。
  要求：
  1. 5~8 条，方向如：主角行为必须符合其性格；身份信息前期不得揭示；反派行为必须有自洽逻辑；重大信息不得一次性倾倒；爽点必须建立在特定结构上；终局信息不得在前期出现；节奏要求等；
  2. 与草案的作者意图、创作约束一致；
  3. 不要写通用的 AI 写作红线（黑名单句式、身体反应套话等），那些是内置的，不需要你生成。
不得输出 JSON 以外的内容。`;
