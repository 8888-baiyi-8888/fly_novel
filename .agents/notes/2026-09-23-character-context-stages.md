# 角色上下文与执行阶段

问题：将 F1 至 F9 全部变成提示片段混淆了资料准备、生成、校验和保存阶段。

决定：只有人物资料、状态、记忆和性格准备返回统一内容片段；外部只传场景和输出字段选择。生成单独执行，校验与整轮记忆状态保存保留独立未实现接口。角色类型放在 character/types.ts。

影响：样例改用 outputRequirements.fields。run 仍返回原始模型状态，未接入后处理；整轮记忆和状态更新保持一致保存边界。

验证：测试编译、类型检查和角色聚焦测试，不访问真实模型。
