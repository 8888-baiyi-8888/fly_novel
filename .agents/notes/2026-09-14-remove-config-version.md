# 移除配置版本字段

## 决定

按用户要求只维护当前配置结构，移除 readDocument 的版本参数和校验，以及样例和本地配置的 version 字段。保留 JSON 对象和凭据字符串校验。

## 影响

仅移除本地文件顶层 version，不改变配置值、凭据密文或加密密钥。配置读取不再解释版本；密文本身仍用 gcm2 标记，旧密文继续在解密时明确拒绝。

## 验证

运行类型检查、构建和测试，检查本地文件去除版本后仍可解析，且其他字段未变化。
