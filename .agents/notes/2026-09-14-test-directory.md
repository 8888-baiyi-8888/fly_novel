# 测试与源码分离

## 问题

测试原先与实现同放在 src，且会进入应用构建产物。

## 决定

测试统一移到根目录 tests，保留 app 与 config 分类。应用构建只编译 src；测试通过 tsconfig.test.json 编译到 .test-dist，并由 npm test 执行。类型检查同时覆盖两者。

## 影响

调整测试导入和文档命令，保留测试断言与应用行为。本次不修改此前审查发现的终端兼容性或换行问题。

## 验证

执行类型检查、应用构建及迁移后的三个测试，确认 src 和 dist 不再包含测试文件。
