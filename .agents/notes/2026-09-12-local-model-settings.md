# 本地模型配置存储

## 问题

配置需要支持当前手工填写与未来前端编辑，且不受启动目录影响。

## 决定

固定 APP_HOME 绝对路径，JSON 普通设置与凭据分开保存，由 config 读取校验，app 传给 Harness。格式版本为 1，未知版本拒绝读取。不实现前端保存、环境变量回退或其他模型适配。

## 影响

文件修改在下次调用生效，迁移机器需修改路径常量。本地文件忽略提交，新克隆需按 README 创建。凭据是明文，模型 HTTP 错误不附响应正文以免反射凭据。

## 验证

检查类型、构建和配置读取行为，使用替身验证模型调用，不使用真实密钥。
