# 工作流文档

本目录存放 `fly_novel` 的核心业务工作流。每个工作流使用独立的 Markdown 文件，文件名采用具体工作流名称的英文小写短横线形式，例如 `chapter-writing.md`。

每篇工作流文档按以下顺序编写：

1. `## 作用`：说明该工作流解决的问题、适用范围和产生的结果。
2. `## 总体流程`：按顺序概述工作流包含的主要阶段。
3. `## 节点`：为每个节点设置三级标题，说明该节点的功能、输入、处理规则、输出和状态变化。

工作流之间共享的概念或状态只在拥有它的文档中完整说明，其他文档链接到对应位置。
