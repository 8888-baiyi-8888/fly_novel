# AGENTS.md

`fly_novel` 是软件开发项目。默认使用简体中文交流和编写文档，代码标识符遵循项目语言惯例。修改代码前先阅读 `README.md`、相关实现及测试；建立架构文档后，架构变更须先阅读该文档。子目录若有 `AGENTS.md`，同时遵循其适用规则。

本文件规定本项目的开发协作方式与工程约束。当前仓库尚未建立源码、依赖和构建体系；下文涉及特定技术或目录的规则，仅在引入相应技术或目录后适用。

## 首次发布前：优先打好基础

**首次正式打标签发布时重新评估本节，并删除仅适用于发布前的规则。** 在用户未要求兼容的前提下，优先修正设计，不为尚未发布的接口堆积兼容层。允许因任务需要重命名、拆分或重新组织模块，但必须同步更新所有引用，不做无关重构。

持久化数据另行处理：格式变化必须明确版本与迁移或拒绝读取策略，不静默误读旧格式，也不以“尚未发布”为由删除用户数据。如果采用数据库模式版本，版本号应单调递增。

**应用启动。** 正式应用、示例和测试应通过各自声明的启动入口运行。建立统一启动方式后，不新增绕过初始化、配置校验或权限控制的隐式入口。

## 仓库结构

当前已存在的项目文件：

```text
AGENTS.md   开发协作与工程约束
README.md   项目介绍
src/       源码目录，包含应用组装、Agent 运行框架、工作流引擎和小说业务（见 src/README.md）
docs/      开发文档目录，存放架构、模块设计、开发流程、决策背景、接口约定和排障记录（见 docs/AGENTS.md）
```

技术栈及模块布局尚未确定。源码、测试、脚本和第三方源码目录应随实际需求建立，并在本节同步登记职责。目录划分应体现本项目的模块职责，不创建无用途的空目录。`docs/` 下新增开发文档时，同时遵循 [docs/AGENTS.md](docs/AGENTS.md)。


### 宿主沙箱失败

必要命令因凭据访问、网络、进程通信、文件监听或子进程限制失败时，先确认是沙箱限制，再通过工具支持的审批机制申请最小必要权限，尽可能保持原命令重试。不得将测试失败当作沙箱问题，也不得绕过应用自身的安全限制。无法执行时，说明具体阻塞及未完成的检查。

### 在本地运行相关检查

- 推送前完成与变更相关的检查；同步分支或解决冲突后，对可能受影响的行为重新验证，检查未通过时不得合并。
- 验证方式应对应变更：业务逻辑用聚焦行为测试，用户可见输出用适当的快照或交互检查，文档用链接和示例检查，发布入口用构建产物冒烟测试，外部服务适配用集成测试。
- 不默认运行全量测试，不因提交或推送重复已通过且输入未变的检查。全量和跨平台验证用于用户明确要求、CI 排障或无法缩小范围的全局变更。
- 普通测试通过不代表覆盖率门禁通过；覆盖率阈值与命令以本项目实际配置为准。
- 只报告实际运行的命令及结果。没有测试体系时明确验证范围，不声称已经通过尚不存在的检查。

## 密钥与 .env

凭据通过项目选定的环境变量或配置机制读取。不得提交真实密钥、令牌、密码、私有证书或包含这些内容的 `.env`；示例配置仅保留字段说明与占位值，并确保真实配置受到忽略规则保护。

引入外部 API 后，记录必需与可选环境变量。真实 API 测试缺少凭据时应明确报告跳过原因，不能伪装成通过；常规测试应能使用固定测试数据或替身在无密钥环境下运行。日志和异常信息不得泄露凭据。

## 工程约定

- **模块约定保持一致。** 包命名、导入方式和运行时版本遵循工程配置。若采用 ESM，源码、启动器与构建产物须保持兼容，不混入依赖隐式转换才能运行的入口。
- **注册必须可清理。** 事件订阅、插件注册、定时器和后台任务应明确所有者与释放方式；注册接口需要时返回取消或清理函数。
- **运行时不变量检查真实关系。** 仅在独立状态可能发生偏离时增加一致性断言，不为固定值或类型已保证的条件添加空洞检查。
- **事件与持久化格式明确版本规则。** 修改事件字段时同步生产方、消费方和序列化逻辑；未知事件或版本应有明确的拒绝或可忽略策略。
- **穷尽处理封闭分支。** 对封闭枚举或可辨识联合执行穷尽检查；可扩展集合保留有说明的默认分支。中间件须明确继续调用与短路返回的语义。
- **沿既有扩展点增加能力。** 若项目采用插件或扩展机制，优先使用其公开接口；改变核心调度或生命周期时，同步更新架构说明。
- **明确接口、实现与调用方的职责。** 只有这些职责确实独立演进时才拆分，不机械引入多层封装。
- **优先采用能减少维护量的成熟依赖。** 引入依赖必须确实减少自有代码与测试负担，并检查兼容性、许可证和维护情况；同步维护清单与锁文件。
- **在模块入口显式解析默认值。** 将输入解析、校验与默认值归一化集中在所属模块，不在执行路径中散布含义不明的回退逻辑。
- **可调参数使用配置。** 随部署变化的超时、地址和容量等必须可以配置并校验；协议常量和安全不变量保持固定。
- **配置错误尽早失败。** 能在加载时发现的问题立即报告，其余在最早可判断的位置报告；不得静默跳过缺失依赖或引用。
- **不同用途的标识符避免混用。** 对容易混淆的跨模块 ID 使用语言支持的专用类型；若采用 TypeScript，可使用品牌类型表达这种区别。
- **信任已验证的内部类型。** 在解析、配置、文件、数据库、网络、进程和工具输入处验证数据，不为同进程内静态类型已保证的值重复添加无意义的回退与测试。
- **源码与构建产物分开。** 源码检查应能在干净工作区运行；消费构建产物的检查必须显式声明构建前置条件，不依赖上次运行残留。
- **编译目标明确。** 如果同时包含服务端、浏览器端或其他运行环境，分别维护必要的编译配置，避免平台依赖相互泄露。
- **不静默吞掉异常。** 确需忽略的异常要限定类型和原因，将捕获范围缩小到必要操作，其他异常继续传播或转换为明确错误。
- **注释紧贴所属代码。** 不重复代码，不无关扩写邻近注释；并列逻辑保持对称，确需不同处理时说明原因。
- **测试描述行为。** 行为改变时同步修改过时断言，并在变更说明中解释原因；不得仅为通过测试削弱有效检查。
- **非简单变更附带 Agent Note。** 涉及架构、跨模块行为、数据格式或重要工程决策时，在同一变更中按需新增 `.agents/notes/` 下的简短记录，说明问题、决定、影响与验证。机械修改、局部文案和简单修复可免除。归档记录保持不变，不作为当前规则使用。
- **界面文案集中管理。** 如果引入国际化，使用统一语言资源和翻译接口，不在组件中散布需翻译的文本；尚无国际化需求时不预建完整体系。
- **用户可见变化有对应验证。** 采用快照体系后，重要输出变化同步更新相关快照；测试数据保持跨平台可重放，不用过度归一化掩盖真实差异。
- **交互展示与功能一起设计。** 新增界面或工具交互时明确加载、成功、失败和取消状态；展示层从明确的数据状态派生，避免在渲染中执行副作用。
- **按风险规划测试。** 跨模块能力、生命周期和用户输出变更需要考虑单元、集成与快照覆盖，只增加与实际行为相关的测试。
- **共享协议同步更新所有客户端。** 若后续存在多个 SDK 或客户端，协议及生命周期变更需同步其实现和预期结果，不能假设主项目测试已覆盖所有客户端。
- **明确管理 PR 历史。** 独立变更尽量拆分；在引入问题的变更中修复后再向后传播。仅在已获授权的历史重写中使用 `--force-with-lease`，远端有新变化时停止，禁止裸用 `--force`。不覆盖用户未完成的修改。
- **标签遵循仓库实际分类。** 建立 PR 或 Issue 分类后按约定填写；是否创建 PR 由任务要求和本项目协作流程决定。
- **待办标记说明具体问题。** `FIXME` 标记已知缺陷，`TODO` 标记后续工作，`XXX` 标记需要重点审查的可疑实现，避免无说明占位。
- **文本格式统一。** 默认 UTF-8，遵循仓库格式配置，文件末尾恰好一个换行。已跟踪修改使用 `git diff --check`，暂存修改使用 `git diff --cached --check`；新文件也应检查空白错误。

## 防御性设计

修改生命周期、并发、子进程或资源释放逻辑前，先阅读已有相关设计文档；如尚无文档，先检查实际实现与调用关系。

明确资源归属、取消传播、超时、重复关闭和失败后的清理行为。清理操作应在需要时可重复执行；异步任务不得在所属对象销毁后继续修改其状态。测试覆盖实际可能发生的竞争与错误路径，不凭空构造内部类型不可能产生的输入。

## 类型安全与文档

若采用 TypeScript，启用 `strict: true` 和 `noImplicitAny`；必须保留的 `any` 应说明为何无法使用更精确的类型。其他语言采用对应的静态检查能力。不得用忽略指令、大范围类型断言或关闭检查掩盖接口问题。

模块和导出接口应说明非显然的使用约定；公共函数说明必要的参数、返回值、失败条件与副作用。继承接口的共同说明放在声明处，不在每个实现中重复。项目采用 JSDoc 时使用相应的 `@param`、`@returns` 标记。

注释与文档使用直接、具体的语言，描述当前行为、失败方式、时序、资源归属和安全使用条件，不记录推理过程、评审对话或逐行复述控制流。优先写“响应字段”“JSON 校验”等确切对象，避免使用模糊术语。设计原因放入对应决策记录并引用。

每次代码修改都检查文档影响，同步更新受影响的 README、接口说明和配置示例。一个事实尽量只有一个维护位置；Markdown 普通段落保持一个物理行。可机械验证的重要约束应纳入实际执行的检查入口，并覆盖相关拒绝路径；需要例外时使用范围明确且有理由的例外，不全局关闭规则。

## 修改本文件

保持每条规则独立可理解，具体技术细节在相应文档建立后通过链接引用。不得添加指向不存在文件的有效性假象，也不得把计划中的命令描述为可运行命令。

只有实际存在符号链接时，才按链接关系编辑真实文件；当前不假定有 `CLAUDE.md` 或与本文件同步的副本。工程结构确定、首次发布或用户调整规范时，更新对应内容，删除过时约束。用户当前的明确指令优先于本文件的项目约定。

## 第三方源码管理

仅在确需内置第三方源码时建立 `vendor/`。记录来源、许可证、固定版本或上游提交，以及更新步骤和本地修改清单。

更新时按记录同步上游，逐项重新应用或移除本地修改，运行受影响的测试与构建。不直接修改内置源码而遗漏变更记录，也不将第三方生成产物误当作本项目源文件维护。
